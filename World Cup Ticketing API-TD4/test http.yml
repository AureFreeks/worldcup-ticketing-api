info:
  name: test http
  type: http
  seq: 4

http:
  method: GET
  url: "{{host}}/test-http"
  auth: inherit

runtime:
  variables:
    - name: host
      value: http://localhost:61856

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
