info:
  name: Filter Match by stage
  type: http
  seq: 11

http:
  method: GET
  url: "{{host}}/matchs/stage/jjf"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
